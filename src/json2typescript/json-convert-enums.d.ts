export declare enum OperationMode {
    DISABLE = 0,
    ENABLE = 1,
    LOGGING = 2,
}
export declare enum ValueCheckingMode {
    ALLOW_NULL = 1,
    ALLOW_OBJECT_NULL = 2,
    DISALLOW_NULL = 3,
}
